<div class="nested-fields">
  <table class='table table-hover table-inverse'>
    <tr>
      <th><%= f.label :name, "材料名" %></th>
      <th><%= f.text_field :name, size: 10 %></th>
      <th><%= f.label :amount, "量" %></th>
      <th><%= f.text_field :amount, size: 3 %></th>
      <th><%= link_to_remove_association "削除", f %></th>
    </tr>
  </table>
</div>